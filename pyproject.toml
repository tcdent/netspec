[project]
name = "netspec"
version = "0.8.0"
description = "Describe a PCB's parts and connections as code, and have the description check itself."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.10,<0.11"]
build-backend = "uv_build"
